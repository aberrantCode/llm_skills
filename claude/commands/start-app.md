---
description: Start the application — discovers startup scripts, selects the right one, executes it, and handles failures. Pass an optional prompt to target a specific stack or component (e.g. /start-app run the backend in prod mode).
---

Use the `start-app` skill to start the application.

Arguments passed to this command (if any): $ARGUMENTS
